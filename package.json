{
  "scripts": {
    "dev-before anything":"node ./build.js watch",
    "so-remember-build": "node ./build.js",
    "server":"python -m http.server 8080"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
